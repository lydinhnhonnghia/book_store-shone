// const session_con = require('../../controllers/session');
// const {
//     refresh_token_middleware,
// } = require('../../middlewares/session');
module.exports = (router) => {
    // router.post('/refresh_token', refresh_token_middleware, session_con.refresh_token);
};
